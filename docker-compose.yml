version: '3.8'

services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: factory-dashboard
    restart: always
    expose:
      - "5173"
    ports:
      - "5173:5173"
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
